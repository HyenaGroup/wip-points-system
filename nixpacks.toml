[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd backend && npm ci']

[phases.build]
cmds = ['cd backend && npm run build || echo "No build step"']

[start]
cmd = 'cd backend && npm run railway:migrate && npm start'
